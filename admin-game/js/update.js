// Admin Game - Update loop
// Minimal update since no character movement

function update() {
    // Admin game has no character, so no movement updates needed
    // All interaction is click-based and handled in create.js
}
